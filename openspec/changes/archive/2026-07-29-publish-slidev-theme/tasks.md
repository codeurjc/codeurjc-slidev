## 1. Fix consumer-root vs. package-root path resolution (in place, no package split yet)

- [x] 1.1 In `vite.config.ts`, change the `slidev-layout-saver` middleware's `layoutDir` resolution from `resolve(import.meta.dirname, 'layouts')` to a consumer-root-relative resolution using `server.config.root`
- [x] 1.2 In `vite.config.ts`, change the `slidev-code-highlight-position-saver` middleware's `slidesPath` resolution from `resolve(import.meta.dirname, 'slides.md')` to a consumer-root-relative resolution using `server.config.root` (also fixed the same latent bug in the pasted-image resolver/`save-image` middleware's `public/` path, same category, not separately tracked)
- [x] 1.3 Verify `server.config.root` is populated identically for both the dev server (`slidev`) and build (`slidev build`) invocation paths by checking both `resolveViteConfigs` call sites in `@slidev/cli`; adjust to use `options.userRoot` via a setup hook instead if it is not reliable in one of the two modes (confirmed via `node_modules/@slidev/cli/dist/serve-bnzg299W.mjs`: `resolveViteConfigs` unconditionally sets `root: options.userRoot` regardless of the `serve`/`build` command; `configureServer` middlewares only run in `serve` mode anyway, where this is exercised)
- [x] 1.4 Confirm `_override/SideEditor.vue` read and the `composables/useEditor.ts` path injected into it remain correctly `import.meta.dirname`-relative (package-owned, unchanged)
- [x] 1.5 Add/update unit or e2e coverage asserting the save-layout and save-code-highlight-position middlewares write to the consumer root even when `vite.config.ts` is invoked from a different physical directory (e.g. simulate via a relocated config or symlink in a test fixture) — added `tests/vite-consumer-root-resolution.spec.ts`, which spins up a plain Vite dev server via `createServer` loading this repo's real `vite.config.ts` with `root` pointed at a throwaway temp directory, and asserts both middlewares write there instead of into the repo; verified it fails against the pre-fix code
- [x] 1.6 Run `pnpm test && pnpm test:e2e` and confirm no regressions (`pnpm test`: 189/189 passed; `pnpm test:e2e`: 55/55 passed, including the new consumer-root-resolution spec)

## 2. Extract the theme package

- [x] 2.1 Decide and set up the workspace layout for the theme package (e.g. `packages/codeurjc-slidev-theme/`) per the design's migration plan, wired via `pnpm-workspace.yaml` (`packages: [., packages/*]`)
- [x] 2.2 Move theme-owned files into the package: `layouts/`, `composables/` (excluding test-only fixtures as appropriate), `setup/`, `_override/SideEditor.vue`, `vite.config.ts`, `uno.config.ts` (moved via `git mv`, history preserved; stray untracked `layout-*.vue` artifacts deleted)
- [x] 2.3 Write the theme package's own `package.json`: name `codeurjc-slidev-theme`, `slidev-theme`/`slidev` keywords per Slidev convention, dependencies (`@vueuse/core`, `markdown-it`, `unocss`); no `@slidev/theme-default` layering needed, `layout: default` is fully self-contained in this theme's own `layouts/default.vue`
- [x] 2.4 Repoint this repo's own root `slides.md` to consume the package as an external consumer would: `theme: codeurjc-slidev-theme` in frontmatter plus a `workspace:*` dependency in the root `package.json`, removing the root's now-redundant local copies (root `package.json` dropped `@slidev/theme-default`, `@vueuse/core`, `markdown-it`, `unocss`, and the theme's dev/test deps, all now owned by the theme package)
- [x] 2.5 Update `e2e/` fixture wiring to reference the package instead of (or in addition to) its current symlink-to-root-files setup; evaluate whether the symlinks are now unnecessary per the design's prediction, but keep them working if not yet proven redundant (confirmed unnecessary: removed `e2e/vite.config.ts`, `e2e/composables/*`, `e2e/setup/*`, `e2e/_override/SideEditor.vue`, `e2e/global-top.vue`, `e2e/layouts/default.vue` symlinks — the theme package now auto-loads via Slidev's roots-merge with zero e2e-side wiring; `e2e/code` and `e2e/public` symlinks kept, unrelated to the theme)
- [x] 2.6 Run `pnpm test && pnpm test:e2e` against the new workspace layout and confirm no regressions (`pnpm test`: 189/189 passed; `pnpm test:e2e`: 56/56 passed). Fixed along the way: (a) `slidev-layout-saver`'s package-template fallback wrote the package's own relative `../composables/...` imports verbatim into the consumer's `layouts/` dir, which doesn't have a `composables/` sibling — now rewritten to the bare `codeurjc-slidev-theme/composables/...` specifier when falling back to the package template; (b) three e2e specs (`code-highlight-callouts`, `code-snippet-import`, `slide-title-carryover`) wholesale-replace `e2e/slides.md` and were missing the new `theme:` frontmatter field, causing Slidev's dev-server "restarting on theme change" behavior to flap and cascade `ERR_CONNECTION_REFUSED` across later tests; (c) `tests/image-position.spec.ts` and one case in `tests/layout-editor.spec.ts` unconditionally read `e2e/layouts/default.vue`, which is no longer seeded (it's now the package's fallback template, only materializing as a consumer-local file after a first edit) — made conditional on `existsSync`, matching the pattern already used elsewhere in `layout-editor.spec.ts`; (d) `e2e/slides.md` had accumulated stale `layout: test-layout-e2e` frontmatter left over from a prior interrupted test run (with no corresponding layout file anywhere, causing an unhandled-exception dev-server crash) — reset to `layout: default`

## 3. Publish the theme package

- [x] 3.1 Add `files`/`exports` (or confirm Slidev's no-build convention is satisfied) in `codeurjc-slidev-theme`'s `package.json` so `.vue`/`.ts` source ships unbuilt (already had a `files` allowlist shipping raw `.vue`/`.ts`; no `exports` map needed since deep imports like `codeurjc-slidev-theme/composables/useEditor` resolve fine without one. Verified via `npm pack --dry-run` and found `composables/__tests__/*.spec.ts` was leaking into the tarball — added `"!composables/__tests__"` to `files` to exclude it; re-verified with `npm pack --dry-run`, now 19 files / 46.4kB, no test files)
- [x] 3.2 Publish `codeurjc-slidev-theme` to npm under the `ivchicano` account (published `codeurjc-slidev-theme@0.1.0`; required the npmjs.com browser-based OTP approval flow rather than a classic `--otp=<code>` code, and had to run `npm publish --access public` from `packages/codeurjc-slidev-theme` itself, not the repo root — running it from root crashed on the root `package.json`'s missing `version` field, since that package is `private: true` and never had one)
- [x] 3.3 Manually smoke-test: scaffold a throwaway directory, add `codeurjc-slidev-theme` as a dependency by hand, write a minimal `slides.md` with `theme: codeurjc-slidev-theme`, and verify `pnpm dev` renders the default layout, a code-highlight callout, and a `<<<` snippet import correctly with no local `vite.config.ts` (scaffolded in a scratch dir with only `package.json` + `slides.md` + `code/Hello.java`, installed the real published `codeurjc-slidev-theme@0.1.0` from the npm registry via plain `npm install`, ran `npx slidev`, and drove it with Playwright: theme auto-loaded with zero local `vite.config.ts`, default layout rendered, both an inline `// [!mark]` callout and a `<<<` snippet-import callout rendered correctly with no leaked marker syntax. Found one real gap along the way: `layouts/default.vue`'s `<img src="/images/logo.png">` compiles to a Vite asset import that hard-crashes ALL slide rendering if the consumer's `public/images/logo.png` doesn't exist — not documented anywhere as a required consumer asset. Had to add a placeholder file to get the smoke test running. This should be addressed by task 4.2 (ship a placeholder logo in the scaffold template) and probably documented in task 5.1; not fixed here since it's template/doc scope, not a publishing defect)

## 4. Build and publish the scaffolding CLI

- [x] 4.1 Create `create-codeurjc-slidev` package mirroring `create-slidev`'s implementation shape: `minimist` for args, `prompts` for the interactive project-name prompt, a bundled `template/` directory, `_gitignore` → `.gitignore` rename handling (downloaded the real published `create-slidev` tarball via `npm pack` to use as a direct implementation reference, then wrote `packages/create-codeurjc-slidev/index.mjs` following its exact shape: `minimist`/`prompts`/`pathe`/`ansis`/`tinyexec`, same non-empty-dir/package-name-validation/README-templating flow, trimmed down where CodeURJC's template doesn't need parity — e.g. only npm/yarn/pnpm as package-manager choices, no Deno/Bun)
- [x] 4.2 Author the bundled template: `package.json` (placeholder name patched at scaffold time, deps on `@slidev/cli` + `codeurjc-slidev-theme`, `dev`/`build`/`export` scripts), starter `slides.md` (`theme: codeurjc-slidev-theme` frontmatter, one example slide), empty `code/` directory (with a `.gitkeep` or similar so it's tracked by git). Also included `template/public/images/logo.png` (copied from this repo's own `public/images/logo.png`) and `template/README.md`, since task 3.3 found that `layouts/default.vue`'s `<img src="/images/logo.png">` hard-crashes all slide rendering when that asset is missing — verified fixed by scaffolding into a scratch dir, `npm install`ing the real published `codeurjc-slidev-theme@0.1.0`, and confirming via Playwright that the logo renders and no crash occurs
- [x] 4.3 Publish `create-codeurjc-slidev` to npm under the `ivchicano` account (published `create-codeurjc-slidev@0.1.0`; user ran the publish command themselves from `packages/create-codeurjc-slidev`, approving the same browser-based OTP flow used for the theme publish)
- [x] 4.4 Smoke-test end to end: `pnpm create codeurjc-slidev demo-talk`, `pnpm install`, `pnpm dev` inside the generated directory, confirm the dev server starts and the starter slide renders with the theme's editor active (ran `npx create-codeurjc-slidev@0.1.0 demo-talk` against the real published registry, then `pnpm install` + `pnpm exec slidev` inside the generated dir; confirmed via Playwright: title renders, logo renders (no crash), the inline code-highlight callout renders, and the layout editor panel opens and is interactive — all from a project containing only `package.json`, `slides.md`, `code/`, and `public/images/logo.png`, no local `vite.config.ts`)

## 5. Documentation

- [x] 5.1 Update this repo's `README.md` to document the theme + scaffold as the primary way to start a new presentation, distinct from cloning this repo (which remains the theme's own development workflow) (rewrote `README.md`: new "Starting a new presentation" section leading with `pnpm create codeurjc-slidev`, links to both published npm packages, expanded feature list to match current reality, and a "Developing the theme itself" section replacing the old flat-repo `Getting started`/`Project structure`/`Development` sections)
- [x] 5.2 Update `CLAUDE.md`/`AGENTS.md` architecture section to reflect the package split (theme package vs. this repo's dev/demo/test fixtures) (`CLAUDE.md` is a symlink to `AGENTS.md`, edited the latter directly: rewrote the overview/architecture section with a workspace-layout list and package-owned-vs-consumer-owned path-resolution explanation, and rewrote Stack/Commands/Tests/Development-cycle to reflect the pnpm workspace, the theme package's actual test location, the removed `e2e/` symlinks, the `theme:` frontmatter requirement for fixture files, and the publish command/gotchas)
