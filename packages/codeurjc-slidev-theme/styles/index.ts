// Slidev doesn't apply this baseline itself -- it's each theme's own
// responsibility to import it (see @slidev/theme-default's styles/index.ts).
// Provides the base `.slidev-layout` heading sizes, paragraph spacing, and
// (notably) `ul`/`ol` `list-style`, which is otherwise just absent.
import '@slidev/client/styles/layouts-base.css'
